<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/dimen_half_of_main"
    android:layout_marginTop="@dimen/dimen_main"
    android:backgroundTint="@color/white"
    android:clickable="true"
    android:foreground="?android:attr/selectableItemBackground"
    app:cardCornerRadius="@dimen/dimen_half_of_main">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/image_recipe"
            android:layout_width="match_parent"
            android:layout_height="@dimen/dimen_iv_item_recipe_image_height"
            android:scaleType="centerCrop"
            tools:src="@drawable/rv_image_stub" />

        <TextView
            android:id="@+id/tv_recipe_title"
            style="@style/StyleTitle14"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginVertical="@dimen/dimen_half_of_main"
            android:layout_marginStart="@dimen/dimen_half_of_main"
            android:textColor="@color/item_title"
            tools:text="Рецепт" />

    </LinearLayout>

</androidx.cardview.widget.CardView>